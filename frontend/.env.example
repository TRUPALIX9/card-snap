# Copy to frontend/.env and fill in. Never commit the real .env.

# Base URL of the Card Snap backend, reachable from the phone on the same network
# (your computer's LAN IP and the backend PORT, no trailing slash)
EXPO_PUBLIC_API_URL=
